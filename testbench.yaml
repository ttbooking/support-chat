laravel: '@testbench'

providers:
  - TTBooking\Nanoid\NanoidServiceProvider
  - TTBooking\ViteManager\ViteServiceProvider
  - TTBooking\SupportChat\SupportChatServiceProvider
  - Workbench\App\Providers\WorkbenchServiceProvider

migrations:
  - workbench/database/migrations

seeders:
  - Workbench\Database\Seeders\DatabaseSeeder
  - TTBooking\SupportChat\Database\Seeders\DatabaseSeeder

env:
  - SC_DISK=local
  - BROADCAST_CONNECTION=reverb
  - REVERB_APP_KEY=3ecd1714239498ef8bae
  - REVERB_APP_SECRET=8ca799435cc151e691c5
  - REVERB_APP_ID=1177139

workbench:
  start: '/support-chat'
  user: test@example.com
  install: true
  health: false
  discovers:
    web: true
    api: true
    commands: true
    components: false
    factories: true
    views: false
  build:
    - asset-publish
    - create-sqlite-db
    - db-wipe
    - migrate-fresh
  assets:
    - laravel-assets
    - workbench-assets
    - support-chat-assets
  sync:
    - from: storage
      to: workbench/storage
      reverse: true
